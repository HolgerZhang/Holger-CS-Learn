### 实验报告 生命游戏

作者：张昊 1927405160

源代码文件夹：Src/

代码语言：C++ （C++11）

文件编码：UTF-8

> 注：`in` 文件为一个配置文件示例

### 编译运行方法

项目基于CMake，可用支持CMake的IDE（如CLion、Visual Studio 2019等）打开并编译运行，也可以在命令行中运行以下命令：

~~~bash
$ cd Src
$ mkdir build
$ cd build
$ cmake ..
$ make
$ ./LifeGame [config-file-path]    # 或为 ./LifeGame.exe [config-file-path]
~~~